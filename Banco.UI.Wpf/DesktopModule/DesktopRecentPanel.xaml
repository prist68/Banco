<UserControl x:Class="Banco.UI.Wpf.DesktopModule.DesktopRecentPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Banco.UI.Wpf.DesktopModule">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:DesktopRecentItemViewModel}">
            <Button Height="34"
                    Margin="0,0,0,6"
                    Padding="8,0"
                    HorizontalContentAlignment="Stretch"
                    Background="{DynamicResource ShellWorkspaceChromeBrush}"
                    BorderBrush="{DynamicResource ShellWorkspaceChromeBorderBrush}"
                    BorderThickness="1"
                    Click="Recent_OnClick">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="48" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="110" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center"
                               FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Text="{Binding TimeLabel}" />
                    <StackPanel Grid.Column="1"
                                VerticalAlignment="Center">
                        <TextBlock FontSize="12"
                                   FontWeight="SemiBold"
                                   Text="{Binding Title}"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock FontSize="10.5"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="{Binding Detail}"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                    <TextBlock Grid.Column="2"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"
                               FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Text="{Binding StateLabel}" />
                </Grid>
            </Button>
        </DataTemplate>
    </UserControl.Resources>

    <Border Style="{StaticResource CardStyle}"
            Background="{DynamicResource WindowBackgroundBrush}"
            Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="8" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock FontSize="16"
                       FontWeight="SemiBold"
                       Text="Attivita` recenti" />
            <ItemsControl Grid.Row="2"
                          ItemsSource="{Binding RecentItems}" />
        </Grid>
    </Border>
</UserControl>
